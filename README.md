# Deno REST API

## Install
```
brew install deno
deno install -qAf --unstable https://deno.land/x/denon/denon.ts
echo 'export PATH="$HOME/.deno/bin:$PATH"' >> ~/.zshrc
```

## Start
```
denon start
```